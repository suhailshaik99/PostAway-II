// import AppError from "../utils/AppError"
// import { Logout } from "../resources/users/user-logout-model.js";
// export const tokenValidator = async (req, res, next) => {
//     if(!req.cookies.jwt) return next(new AppError("Please login to use this feature...", 401));
//     const tokenStatus = Logout.findOne

// }